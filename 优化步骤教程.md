# 优化步骤教程：把 SSD streaming 的专家预取跑满

本机环境：RTX 5060 Ti（可用 15.48 GiB）、~90 GiB RAM、NVMe（Fanxiang S910Pro 2TB）、
模型 `DeepSeek-V4.1-Flash-Q2.gguf`（340.60 GiB，40 层，384 路由专家，9.49 MiB/专家，top-6）。
每解码一个 token 要搬 2.28 GB 专家权重（40 层 × 6 专家 × 9.49 MiB），所以优化目标只有一个字：
**单位时间从盘上拿到更多字节**。

## 1. 构建

```sh
make cuda                 # 不给 CUDA_ARCH 时自动用 nvidia-smi 探测（本机 sm_120）
make tests/test_cuda_ssd_cache   # 回归测试二进制，改完必须重建，否则还在跑旧代码
```

坑：`make cuda` 只重建 `ds4*` 主程序，**不重建测试**。改了 `ds4_cuda.cu` 后必须单独
`make tests/test_cuda_ssd_cache`，否则会拿着旧二进制的挂死现象误判。

## 2. 先找到一条能跑通的命令

```sh
./ds4 --cuda -m /data/ai/models/gguf/DeepSeek-V4.1-Flash-Q2.gguf --ssd-streaming \
      --ssd-streaming-cache-experts 41 -c 2048 --prefill-chunk 512 --nothink \
      -p "Explain what mmap is, briefly." -n 64
```

两个必须知道的坑：

- 缓存目标要写 **slot 数**（`41`），不要写 `1GB`。`1GB` 被解释为“额外预留两个完整 prefill 层”，
  结果是 `0.99 GiB prefill headroom + 0.01 GiB 动态缓存 = 1 个 slot`，随后
  `CUDA SSD cache cannot stage 254 experts with system headroom` 直接失败。
- prompt 别太长：prefill 会为每个“唯一路由专家 × 3 个张量”占用缓存槽，293 token 的 prompt 大约
  需要 254 个槽。基准测试用短 prompt 即可，长 prompt 要同步放大缓存。

另外 `ds4-bench` 的预算算法和 `ds4` 不一样，同样的 `1GB` 它会塌成 1 个 slot，还会要求
prompt 不少于 `--ctx-start`；做解码对比建议直接用 `ds4` 一次性生成。

## 3. 先量基线，再改代码

```sh
DS4_CUDA_DISABLE_EXPERT_PARALLEL_READ=1 ./ds4 --cuda ... -n 64   # 串行基线：1.93 t/s
```

**任何改动都要和这条基线对比**，否则会把环境波动当成收益。同一条命令跑两遍，差异在几个百分点以内
才可信。

## 4. 扫并发深度

```sh
for d in 4 6 8 10 12 16; do DS4_CUDA_EXPERT_READ_DEPTH=$d ./ds4 --cuda ... -n 64; done
```

判据：深度从 4 涨到 8 明显变快，8 以上基本平台（本机 6/8/10/12/16 都在 3.74–3.80 t/s）。
平台出现即定默认值，不要把上限继续往上堆——更多并发只是多占 pinned 内存。

## 5. 架构：为什么是滑动窗口

- 串行：一个 pread ~5 GB/s，整条 token 时间被单个 IO 延迟占满。
- 分批栅栏：一批 N 个请求齐了才发拷贝、才补下一批，最慢的那个 pread 拖住其余 N-1 个
  reader（本机 @16 只到 3.21 t/s）。
- 滑动窗口：**谁回来就立刻上拷、立刻给它补新请求**，读队列始终满（@8 到 3.80 t/s）。

不变式（改这块时最容易踩）：

- 一个 reader 槽 = 一个 staging buffer + 一个完成事件；该槽复用前必须
  `cudaEventSynchronize` 自己的事件，否则上一笔 H2D 拷贝还没读完就被覆盖。
- 所有 reader 共用同一个条件变量 → 派活必须用 `pthread_cond_broadcast`；`signal` 会唤醒
  没活干的 worker，唤醒被吞掉，请求永远没人读。
- 派活票据要延续 worker 已服务的序号；每次调用从 1 重开会撞号，worker 认为无事可做。
- 异常/中断退出时，必须等仍在读取的 reader 回来，再释放 staging buffer 和 fd。

## 6. 挂死怎么查（无 gdb 的环境）

`ptrace_scope` 不允许 gdb attach 时用 /proc：

```sh
pgrep -af "ds4 --cuda"                      # ds4 有单实例锁，遗留进程会让后续运行直接报“already running”
pid=$(pgrep -f "^./ds4 --cuda" | head -1)
ls /proc/$pid/task | wc -l                  # 线程数：主线程 + N 个 reader + CUDA poll 线程
for t in /proc/$pid/task/*; do echo "$(basename $t) $(cat $t/wchan)"; done
ps -o time= -p $pid                         # CPU 时间几乎不涨 + 全部 futex_wait = 丢唤醒/死锁
```

定位不了就在等待循环里加**超时诊断**：`pthread_cond_timedwait` 5 s 超时后打印每槽
`posted / answered / expect / busy / phase / served`，一眼就能看出是“worker 在等活”还是
“main 在等回包”。诊断代码确认后删掉。

## 7. 回归

```sh
./tests/test_cuda_ssd_cache     # 全部 PASS 约 5 秒；覆盖读失败、部分批次、prefetch 拆栈、FD 拆卸
```

这个测试在预取池出问题时是**卡死**而不是报错，所以它比任何基准都更早发现问题。

## 8. 剩下的杠杆（按预期收益）

1. **更小的 checkpoint**：`/data/ai/models/gguf/DeepSeek-V4-Flash-IQ2XXS-...0731.gguf`（45.5 GiB），
   每 token 流量约少 7.5 倍。
2. **batch 解码**：同批 token 复用同一层的专家权重，提升总吞吐（单流延迟不变）。
3. **加大读透缓存的覆盖面**：默认只给可用内存的 1/3，剩下的 81 GiB/128 token 仍走盘；
   但预算扫描显示 32 GiB 之后基本没有收益，加大之前先按第 9 节的办法量清楚你自己的曲线。

**本机已到上限、不要再试的**：增大专家显存缓存（工作集约 15360 专家，逐 token 复用≈0）、
`--mtp`/`--dspark`（V4.1 CUDA 直接拒绝）、把专家权重做无损压缩
（iq2_xxs/q2_k 熵 7.9/8，压缩比 1.00–1.02x）、SSD→内存跨层预取
（解码已在盘带宽上限：每 token 2.39 GB × 3.80 t/s ≈ 9.07 GB/s，接近裸盘 8.7 GB/s）、
把 MoE 挪到 CPU（ds4 CPU 后端 0.06 t/s，内存带宽实测 34 GB/s）。

## 9. 已经做完的（本机当前默认就开着）

- **专家读取滑动窗口并发**（默认深度 8）：V4.1 Q2 解码 1.93 → 3.80 t/s。见第 4、5 节。
- **`--ram-resident-experts auto`（默认）**：路由专家装得进可用内存就一次性 pin 进主机内存，
  此后的 staged read 全部变成 H2D 拷贝。V4 Flash IQ2XXS 7.22 → 13.02 t/s；
  Q2 装不下会自动跳过并打印原因。对照组：`DS4_RAM_RESIDENT_EXPERTS=0`。
- **读透式主机专家缓存**（Q2 这类装不下的模型，默认开）：读过的专家留在 pinned 内存，
  命中走 H2D。Q2 解码 3.85 → 6.30 t/s，落盘字节 316 → 89 GiB（命中率 73.7%）。
  预算默认可用内存的 1/3（上限 32 GiB），`DS4_HOST_EXPERT_CACHE=off|NGB` 覆盖，
  `DS4_HOST_EXPERT_CACHE_STATS=1` 打命中率。
- **`--host-offload-token-embd`**：token_embd 挪出显存，省约 1.2 GiB（给上下文/prefill/OOM，
  **不会**变成更多专家槽）。

复现这些数字的命令（预算扫描、页缓存对照实验）与每一条的决策依据见
`docs/OPTIMIZATION_HISTORY.md`。

## 10. 量自己的缓存曲线

```sh
M=/data/ai/models/gguf/DeepSeek-V4.1-Flash-Q2.gguf
C=(-m "$M" --cuda --ssd-streaming --ssd-streaming-cache-experts 41 -c 2048 \
   --prefill-chunk 512 --nothink --temp 0 -n 128 -p "Explain what mmap is, briefly.")
for b in off 8 16 32 48; do
  DS4_HOST_EXPERT_CACHE=$b DS4_HOST_EXPERT_CACHE_STATS=1 ./ds4 "${C[@]}"
done
```

同时后台采样 `/proc/<pid>/io` 的 `read_bytes` 得到落盘字节，并把生成的正文做 md5：
**任何预算下的 md5 都必须和 `off` 一致**，否则就是缓存把字节给错了。

注意：`ds4` 有单实例锁，同一时刻只能跑一个实例；脚本里连着跑多组时要能用
`flock -n /tmp/ds4.lock` 拿到锁再启动，否则那次运行会直接退出。
